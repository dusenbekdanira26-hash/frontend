# Lab 03 — Combined App

This app combines Task 1 (StepCounter) and Task 2 (UserProfile) into one Vite project.

Run:

```powershell
cd "c:/Users/dyuse/OneDrive/Рабочий стол/lab_3/Lab_03/combined"
npm install
npm run dev
```

UI: tabs switch between Counters (reuses `task1` components) and User Profile (local component).
